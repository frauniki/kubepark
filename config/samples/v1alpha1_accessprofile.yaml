apiVersion: kubepark.dev/v1alpha1
kind: AccessProfile
metadata:
  labels:
    app.kubernetes.io/name: kubepark
    app.kubernetes.io/managed-by: kustomize
  name: accessprofile-sample
spec:
  # TODO(user): Add fields here
